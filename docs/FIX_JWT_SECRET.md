# Исправление ошибки JWT Secret

## Проблема

При попытке входа через Flutter приложение возникает ошибка:

```
TypeError: Expected a string value
```

В логах backend:
```
File "/app/app/cabinet/auth/jwt_handler.py", line 40, in create_access_token
    return jwt.encode(payload, secret, algorithm=JWT_ALGORITHM)
...
File "/opt/venv/lib/python3.13/site-packages/jwt/utils.py", line 22, in force_bytes
    raise TypeError("Expected a string value")
```

## Причина

JWT encoding требует строковый секрет, но получает `None`. Это происходит когда:

1. `CABINET_JWT_SECRET` не установлен в `.env`
2. `BOT_TOKEN` не установлен (API-only режим с `TELEGRAM_BOT_ENABLED=false`)
3. Fallback логика возвращает `None`

### Как это работает

Backend использует следующую логику для получения JWT секрета:

```python
def get_cabinet_jwt_secret(self) -> str:
    # Приоритет 1: CABINET_JWT_SECRET
    if self.CABINET_JWT_SECRET:
        return self.CABINET_JWT_SECRET
    
    # Приоритет 2: BOT_TOKEN
    if self.BOT_TOKEN:
        return self.BOT_TOKEN
    
    # Приоритет 3: Auto-generated (fallback)
    return generate_random_secret()
```

**Важно**: После исправления (с версии после коммита), backend автоматически генерирует случайный секрет если оба значения не установлены. Это решает проблему для development, но **не рекомендуется для production**.

---

## Решение

### Способ 1: Установить CABINET_JWT_SECRET (рекомендуется)

#### Шаг 1: Сгенерировать секрет

```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

Пример вывода:
```
Q7x9K2mP4vN8rL3sW6aZ1bY5cT0hJ4uR2gF7eD9iX8kV3nM5qA1wS4yB6oC2pU7z
```

#### Шаг 2: Добавить в .env

```bash
# Добавить строку в .env
echo "CABINET_JWT_SECRET=Q7x9K2mP4vN8rL3sW6aZ1bY5cT0hJ4uR2gF7eD9iX8kV3nM5qA1wS4yB6oC2pU7z" >> .env
```

Или отредактировать вручную:

```env
# .env
CABINET_JWT_SECRET=Q7x9K2mP4vN8rL3sW6aZ1bY5cT0hJ4uR2gF7eD9iX8kV3nM5qA1wS4yB6oC2pU7z
```

#### Шаг 3: Перезапустить backend

**Docker**:
```bash
docker-compose -f docker-compose.local.yml down
docker-compose -f docker-compose.local.yml up -d
```

**Без Docker**:
```bash
# Остановить (Ctrl+C)
python main.py
```

---

### Способ 2: Использовать BOT_TOKEN

Если вы используете Telegram бота (`TELEGRAM_BOT_ENABLED=true`), можно не устанавливать `CABINET_JWT_SECRET` - будет использован `BOT_TOKEN`:

```env
# .env
TELEGRAM_BOT_ENABLED=true
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
# CABINET_JWT_SECRET не установлен - будет использован BOT_TOKEN
```

⚠️ **Примечание**: Этот способ не работает в API-only режиме (`TELEGRAM_BOT_ENABLED=false`).

---

### Способ 3: Auto-generated секрет (dev only)

После последнего исправления, backend автоматически генерирует случайный секрет если оба значения не установлены:

```env
# .env
TELEGRAM_BOT_ENABLED=false
# CABINET_JWT_SECRET не установлен
# BOT_TOKEN не установлен
```

**Просто перезапустить backend**:
```bash
docker-compose -f docker-compose.local.yml restart bot
```

Backend сгенерирует случайный секрет и выведет warning в логи:

```
⚠️  CABINET_JWT_SECRET and BOT_TOKEN are not set! 
Using auto-generated secret. This is NOT recommended for production!
Please set CABINET_JWT_SECRET in your .env file.
```

#### ⚠️ Ограничения auto-generated секрета:

1. **Токены становятся невалидными после перезапуска**
   - Секрет генерируется заново при каждом запуске
   - Все существующие access/refresh токены станут недействительными
   - Пользователям нужно будет войти заново

2. **Не подходит для production**
   - Нестабильно
   - Пользователи будут разлогинены при каждом restart
   - Невозможно использовать multiple instances

3. **Только для development**
   - Быстрое локальное тестирование
   - Проверка функционала
   - Не требует дополнительной настройки

---

## Проверка работы

### 1. Проверить логи backend

```bash
# Docker
docker logs remnawave_bot 2>&1 | grep -i "jwt\|secret"

# Без Docker
# Посмотреть вывод в консоли
```

**Что искать**:
- ✅ Нет ошибок про JWT
- ⚠️ Если видите warning про auto-generated - установите CABINET_JWT_SECRET

### 2. Проверить вход через Flutter

```bash
cd flutter_app/ulya_vpn
flutter run --dart-define=API_BASE_URL=http://10.0.2.2:8081
```

Попробовать:
1. Зарегистрироваться
2. Войти

**Результат**: Должно работать без ошибок ✅

### 3. Проверить через curl

```bash
# Регистрация
curl http://localhost:8081/cabinet/auth/email/register/standalone -X POST \
  -H "Content-Type: application/json" \
  -d '{
    "email":"test@example.com",
    "password":"TestPass123!",
    "first_name":"Test",
    "last_name":"User",
    "language":"ru"
  }'

# Должен вернуть токены или сообщение о регистрации
```

---

## Конфигурация для разных сценариев

### Production (обязательно)

```env
CABINET_ENABLED=true
CABINET_JWT_SECRET=<сгенерированный-секрет-32+-символов>
CABINET_EMAIL_AUTH_ENABLED=true
WEB_API_ENABLED=true
```

**Почему важно**:
- Стабильность токенов
- Безопасность
- Multiple instances support
- Нет logout при restart

### Development с Telegram ботом

```env
TELEGRAM_BOT_ENABLED=true
BOT_TOKEN=123456:ABC...
CABINET_ENABLED=true
CABINET_EMAIL_AUTH_ENABLED=true
WEB_API_ENABLED=true
# CABINET_JWT_SECRET не установлен - используется BOT_TOKEN
```

### Development API-only

**Вариант 1 (рекомендуется)**:
```env
TELEGRAM_BOT_ENABLED=false
CABINET_ENABLED=true
CABINET_JWT_SECRET=<любой-секрет-для-dev>
CABINET_EMAIL_AUTH_ENABLED=true
WEB_API_ENABLED=true
```

**Вариант 2 (быстрое тестирование)**:
```env
TELEGRAM_BOT_ENABLED=false
CABINET_ENABLED=true
# CABINET_JWT_SECRET не установлен - используется auto-generated
CABINET_EMAIL_AUTH_ENABLED=true
WEB_API_ENABLED=true
```

⚠️ При варианте 2 пользователи будут разлогинены после каждого restart.

---

## Итоговый чек-лист

- [ ] Backend запущен
- [ ] Проверены логи (нет ошибок JWT)
- [ ] Для production: `CABINET_JWT_SECRET` установлен
- [ ] Для dev с ботом: `BOT_TOKEN` установлен
- [ ] Для dev API-only: `CABINET_JWT_SECRET` установлен или используется auto-generated
- [ ] Backend перезапущен после изменений в .env
- [ ] Вход через Flutter работает без ошибок
- [ ] Токены не становятся невалидными (если не используется auto-generated)

---

## FAQ

**Q: Можно ли использовать один и тот же секрет на dev и production?**  
A: Не рекомендуется. Используйте разные секреты для разных окружений.

**Q: Что делать если я изменю CABINET_JWT_SECRET на production?**  
A: Все существующие токены станут невалидными. Пользователям нужно будет войти заново.

**Q: Насколько длинным должен быть секрет?**  
A: Рекомендуется минимум 32 символа. Используйте `secrets.token_urlsafe(32)` для генерации.

**Q: Можно ли использовать простой пароль как секрет?**  
A: Технически да, но не рекомендуется. Используйте криптографически стойкий случайный секрет.

**Q: Auto-generated секрет безопасен?**  
A: Да, он генерируется криптографически безопасно. Но он не персистентен - меняется при каждом запуске.

---

## Дополнительная информация

**Связанные документы**:
- `docs/API_ONLY_MODE.md` - Настройка API-only режима
- `docs/FLUTTER_CONNECTION.md` - Подключение Flutter к backend
- `docs/BACKEND_READY_CHECKLIST.md` - Финальная проверка готовности

**Если проблема не решена**:
1. Проверьте все переменные в `.env`
2. Убедитесь что backend перезапущен после изменений
3. Проверьте логи на другие ошибки
4. Попробуйте сгенерировать новый секрет

Все должно работать! 🔐✅
