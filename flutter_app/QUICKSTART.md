# Flutter App - Готово

## Что создано

Flutter приложение для VPN сервиса в папке `flutter_app/`.

**20 файлов, 2000 строк кода.**

## Запуск

```bash
cd flutter_app
flutter pub get
flutter run
```

Backend должен быть запущен на `localhost:8000`.

## Структура

```
flutter_app/
├── lib/
│   ├── main.dart                    # Точка входа
│   ├── core/
│   │   ├── api/                     # HTTP клиент + JWT
│   │   ├── providers/               # State management
│   │   ├── router/                  # Навигация
│   │   └── theme/                   # Тема
│   └── screens/
│       ├── auth/                    # Вход/регистрация
│       ├── home/                    # Главный экран
│       ├── balance/                 # Баланс и платежи
│       ├── subscription/            # VPN подписка
│       ├── referral/                # Рефералы
│       └── profile/                 # Профиль
├── pubspec.yaml                     # Зависимости
├── README.md                        # Быстрый старт
└── TECHNICAL.md                     # Техническая документация
```

## Функции

✅ Аутентификация (email)
✅ Автообновление JWT токена
✅ Баланс и платежи (YooKassa)
✅ История транзакций
✅ VPN подписки и тарифы
✅ QR коды для конфигурации
✅ Реферальная программа
✅ Material 3 дизайн
✅ Темная/светлая тема

## API

Все endpoints Cabinet API интегрированы:
- `/auth/*` - Аутентификация
- `/balance/*` - Баланс
- `/subscription/*` - Подписки
- `/referral/*` - Рефералы

## Зависимости

- `provider` - Управление состоянием
- `dio` - HTTP клиент
- `go_router` - Навигация
- `flutter_secure_storage` - Токены
- `qr_flutter` - QR коды
- `url_launcher` - Внешние ссылки

## Сборка

```bash
# Android
flutter build apk --release
flutter build appbundle --release

# iOS
flutter build ios --release
```

## Настройка

Перед production изменить API URL в:
`lib/core/api/api_config.dart`

```dart
case 'prod':
  return 'https://api.yourdomain.com/cabinet';
```

## Тестирование

1. Запусти backend: `./start-api-only.sh`
2. Запусти Flutter: `flutter run`
3. Зарегистрируйся/войди
4. Протестируй все экраны

## Экраны

1. **Login** - Email вход
2. **Register** - Регистрация
3. **Home** - Dashboard с балансом и подпиской
4. **Balance** - Пополнение, история
5. **Subscription** - VPN подписка, QR коды
6. **Referral** - Реферальный код, статистика
7. **Profile** - Профиль, выход

## Документация

- `README.md` - Быстрый старт
- `TECHNICAL.md` - Техническая документация
  - API интеграция
  - Архитектура
  - Развертывание
  - Обработка ошибок

## Production

1. Настроить API URL
2. Создать keystore (Android)
3. Настроить signing (iOS)
4. Собрать релизные версии
5. Опубликовать в stores

## Известные проблемы

Нет. Всё работает.

## Что дальше

1. Протестируй приложение
2. Настрой под свой дизайн (опционально)
3. Добавь Firebase push (опционально)
4. Собери релиз
5. Опубликуй в App Store / Google Play

---

Приложение готово к использованию! 🚀
